{
	"name": "@zaydek/prose",
	"version": "0.2.4",
	"main": "dist/index.css",
	"license": "MIT",
	"files": [
		"dist/"
	],
	"scripts": {
		"build-prose": "yarn -s sass src/index.scss dist/index.css && yarn -s sass -s compressed src/index.scss dist/index.min.css",
		"build-reset": "yarn -s sass src/tailwind-base.scss dist/reset.css && yarn -s sass -s compressed src/tailwind-base.scss dist/reset.min.css",
		"build": "yarn build-prose && yarn build-reset",
		"publish-dist-dry-run": "yarn build && npm publish --dry-run"
	},
	"devDependencies": {
		"prettier": "^2.1.2",
		"sass": "^1.28.0",
		"watch": "^1.0.2"
	}
}
